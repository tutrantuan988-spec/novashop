version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: novashop-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: novashop_db
      POSTGRES_USER: novashop_user
      POSTGRES_PASSWORD: novashop_password_dev
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-postgres.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U novashop_user -d novashop_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - novashop-network

  # Redis for caching (Phase 4)
  redis:
    image: redis:7-alpine
    container_name: novashop-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - novashop-network
    command: redis-server --appendonly yes

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  novashop-network:
    driver: bridge
